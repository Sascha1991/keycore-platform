# KS-05-03 Implementation Report

## Summary

Implemented the WooCommerce storefront publication foundation on branch `feature/ks-05-03-woocommerce-publication`.

## Files Changed

- `.env.example`
- `CHANGELOG.md`
- `packages/platform/src/contracts.ts`
- `packages/platform/src/domain/audit.ts`
- `packages/platform/src/storefront/publication.ts`
- `packages/platform/src/storefront/publication.test.ts`
- `infra/storefront/in-memory-publication-repository.ts`
- `infra/storefront/woocommerce-adapter.ts`
- `infra/storefront/woocommerce-adapter.test.ts`
- `infra/postgres/storefront-publication-repositories.ts`
- `infra/postgres/storefront-publication-persistence.test.ts`
- `infra/postgres/persistence.test.ts`
- `infra/postgres/migrations/005_storefront_publication.up.sql`
- `infra/postgres/migrations/005_storefront_publication.down.sql`
- `tasks/phase-05-catalog/KS-05-03-woocommerce-publication.md`
- `docs/storefront-publication.md`
- `docs/implementation-reports/KS-05-03.md`

## Policy Version

`storefront-publication-v1`

## Publication State Model

States:

- `NOT_PUBLISHED`
- `PENDING_CREATE`
- `PUBLISHED`
- `PENDING_UPDATE`
- `UNPUBLISH_PENDING`
- `UNPUBLISHED`
- `BLOCKED`
- `FAILED`
- `REVIEW_REQUIRED`

Ambiguous create outcomes, pending creates and local persistence failure after remote create move to reconciliation-required behavior and prevent blind duplicate WooCommerce creation.

PR #10 hardening added explicit transport-ambiguity handling:

- `POST` network disconnects, connection resets, aborts, timeouts and unreadable/truncated responses are classified as `StorefrontAmbiguousError`;
- ambiguous create becomes `RECONCILE_AMBIGUOUS_CREATE` and does not become `FAILED_REMOTE_CREATE`;
- automatic retry after ambiguous create returns reconciliation again and does not issue a second `POST`;
- deterministic HTTP rejection such as auth/client errors remains a normal create failure;
- ambiguous `PUT` update and soft-unpublish become `RECONCILE_AMBIGUOUS_UPDATE` and `RECONCILE_AMBIGUOUS_UNPUBLISH`.

When a remote WooCommerce product is created and the local publication persistence write fails, the service preserves the known remote product ID in the reconciliation record if the repository is available for that reconciliation write. If the repository is completely unavailable, the operation stops and requires operational reconciliation without blind title search or another create.

Audit environment is injected via `StorefrontPublicationServiceOptions.environment` and uses the existing audit environment values: `LOCAL`, `CI`, `STAGING` and `PRODUCTION`. The service does not infer environment from storefront channel or domain.

CI run #33 exposed that `003_catalog_sync_foundation.down.sql` was not fully idempotent when later rollback paths had already removed `supplier_offers` or `supplier_products`. The PR #10 hardening commits make those down-migration `ALTER TABLE` statements use `IF EXISTS` and make the base `001_initial_schema.down.sql` full tear-down resilient to later dependency objects. They do not change any up migration, uniqueness constraint or storefront publication schema.

CI run #34/#35 exposed an intermittent PostgreSQL integration-test race around database-global `CREATE EXTENSION IF NOT EXISTS pgcrypto` while multiple test suites applied migrations concurrently in isolated schemas. The fix adds `infra/postgres/test-database.ts`, a shared test bootstrap that:

- opens an independent PostgreSQL client per test schema;
- acquires a stable KeyCore-specific database advisory lock with `pg_advisory_lock(1262702674, 1283)`;
- runs `CREATE EXTENSION IF NOT EXISTS pgcrypto` inside `try/finally`;
- releases the lock with `pg_advisory_unlock(1262702674, 1283)`;
- creates the isolated schema only after the database-global extension prerequisite is stable;
- applies migrations with schema-local `keycore_migrations` tracking;
- rolls back only migrations that were actually applied;
- drops the isolated schema as final cleanup and closes the client.

The helper is used by:

- `infra/postgres/persistence.test.ts`;
- `infra/postgres/catalog-persistence.test.ts`;
- `infra/postgres/canonical-product-persistence.test.ts`;
- `infra/postgres/storefront-publication-persistence.test.ts`.

Regression coverage in `infra/postgres/test-database.test.ts` initializes two independent schemas concurrently and proves both schemas initialize, `pgcrypto` remains database-global, migration tracking remains schema-local, data remains schema-isolated and cleanup of one schema does not damage the other.

## WooCommerce Contract

The WooCommerce adapter is behind `StorefrontPort` and uses documented REST API `wc/v3` product endpoints:

- create: `POST /wp-json/wc/v3/products`;
- read: `GET /wp-json/wc/v3/products/<id>`;
- update: `PUT /wp-json/wc/v3/products/<id>`;
- soft-unpublish: `PUT /wp-json/wc/v3/products/<id>` with draft, hidden catalog visibility and out-of-stock status.

No hard delete endpoint is used.

References:

- [WooCommerce REST API documentation](https://developer.woocommerce.com/docs/apis/rest-api/)
- [WooCommerce products REST API documentation](https://developer.woocommerce.com/docs/apis/rest-api/v3/products/)

## Persistence

Migration `005_storefront_publication` creates durable storefront publication mappings and enforces:

- unique `ProductId + storefront`;
- unique `storefront + remote_product_id` where a remote ID exists;
- foreign-key ownership by `products(id)`;
- checked publication states.

The down migration drops the new indexes and table without rewriting supplier, catalog, order or WooCommerce state.

## Dependency Versions Selected

No new npm, Composer or Docker dependency versions were added. The implementation uses existing Node.js, TypeScript, PostgreSQL, Vitest and built-in `fetch`/`Buffer` capabilities.

## Commands and Tests Executed

- `npm.cmd run typecheck`: passed during implementation.
- `npm.cmd run check`: passed.
  - format check: passed.
  - lint: passed.
  - typecheck: passed.
  - Vitest: 303 passed, 38 skipped across 20 test files.
  - secret scan: passed.
- `npm.cmd audit --audit-level=low`: passed, 0 vulnerabilities.
- `npm.cmd run secrets:scan`: passed.
- `npm.cmd test -- infra/postgres`: 3 passed, 33 skipped locally because `KEYCORE_TEST_DATABASE_URL` is not configured.

PostgreSQL and Redis integration tests remain skip-gated locally unless `KEYCORE_TEST_DATABASE_URL` and `KEYCORE_TEST_REDIS_URL` point at running local services. No live WooCommerce store was contacted.

## Post-Push Status

Commit `7c29ff284969494976a28d17a05a14796d2f56a3` was pushed to branch `feature/ks-05-03-woocommerce-publication`.

The connected GitHub integration returned `403 Resource not accessible by integration` when creating the pull request, and the local `gh` CLI is not installed. No PR-triggered GitHub Actions workflow run exists yet for this commit. Direct PR creation URL:

https://github.com/Sascha1991/keycore-platform/pull/new/feature/ks-05-03-woocommerce-publication

## Acceptance Criteria Status

- Active canonical product with at least one active Germany `ALLOWED` offer can publish: satisfied.
- Unsafe grouping state, inactive product, missing required fields, missing price and no eligible Germany offer block publication: satisfied.
- Existing remote mapping is used for updates: satisfied.
- Mapping conflicts fail closed: satisfied.
- Ambiguous remote create and local persistence failure after remote create require reconciliation: satisfied.
- WooCommerce POST transport uncertainty cannot become an ordinary create failure or trigger a blind second create: satisfied.
- Audit environment is configured and not hardcoded to CI: satisfied.
- Eligibility loss soft-unpublishes without WooCommerce hard delete: satisfied.
- Payloads exclude supplier costs, credentials, product keys, customer data and raw supplier payloads: satisfied.
- PostgreSQL migration is reversible: satisfied.

## Known Limitations

- No live WooCommerce connection was used.
- No production domain or credential was used.
- No checkout, payment, procurement or fulfillment behavior was implemented.
- No GAMIVO adapter was implemented.
- No admin UI for reconciliation was implemented.

## Human Review Needed

- Review the publication state names and reason codes before merge.
- Review local WooCommerce placeholder naming before real environment provisioning.
- Review whether future KS-05-04 should add an explicit reconciliation admin workflow.

## Scope Confirmation

- KS-05-04 was not started.
- No live WooCommerce store was contacted.
- No real WooCommerce credentials were committed.
- `.env` remains uncommitted and absent from Git history.
- No payment, checkout, procurement, fulfillment, GAMIVO or product-key behavior was implemented.
- No production customer/order data was used.
