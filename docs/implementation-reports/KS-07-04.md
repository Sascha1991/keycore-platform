# KS-07-04 Implementation Report

## Summary

Implemented the secure key retrieval and fulfillment foundation on branch
`feature/ks-07-04-secure-key-fulfillment`.

This task did not execute live key retrieval, did not retrieve the real key for
`GE1373B866F3`, did not display any product key and did not perform customer
delivery.

## Kinguin Documentation Findings

Official Kinguin documentation was re-checked for Order v2, Order v1, error
codes and return-key behavior.

Verified:

- purchased keys are retrieved through `GET /v2/order/{orderId}/keys`;
- authentication uses `X-Api-Key`;
- the key retrieval endpoint supports `page` and `limit`;
- output is an array of key objects with `id`, `serial`, `type`, `name`,
  `kinguinId`, `offerId` and `productId`;
- `serial` is product-key material;
- order status `processing` means the order is waiting for key delivery;
- order status `completed` means all keys have been delivered;
- key statuses include `PENDING`, `PROCESSING`, `DELIVERED`, `RETURNED`,
  `REFUNDED` and `CANCELED`;
- the docs recommend periodic key download using the Download keys endpoint with
  pagination, using `order.status` webhook when status becomes `completed`, or
  polling order details until keys are delivered;
- `POST /v2/order/{orderId}/keys/return` is a separate state-changing return
  operation with a separate one-request limit and is not used by KS-07-04.

KS-07-04 therefore models Kinguin key retrieval as repeatable read-only. The
evidence is stronger than HTTP method alone: Kinguin's docs tell clients to
periodically call the key download endpoint, while state mutation is documented
on a separate return endpoint. Network, timeout and rate-limit failures remain
retryable only through a later explicit invocation governed by the durable
fulfillment lease. Malformed key responses fail closed.

Sources:

- <https://github.com/kinguinltdhk/Kinguin-eCommerce-API/blob/master/api/order/v2/README.md>
- <https://github.com/kinguinltdhk/Kinguin-eCommerce-API/blob/master/api/order/v1/README.md>
- <https://github.com/kinguinltdhk/Kinguin-eCommerce-API/blob/master/api/ErrorsCodes.md>
- <https://github.com/kinguinltdhk/Kinguin-eCommerce-API/blob/master/features/ReturnKeys.md>

## Fulfillment State Model

Fulfillment states:

- `PENDING`
- `READY`
- `RETRIEVAL_IN_FLIGHT`
- `RETRIEVED`
- `DELIVERY_PENDING`
- `DELIVERED`
- `FAILED_RETRYABLE`
- `FAILED_TERMINAL`
- `AMBIGUOUS`
- `MANUAL_REVIEW_REQUIRED`

Retrieval and delivery are separate. A successfully encrypted supplier key ends
at `DELIVERY_PENDING`; KS-07-04 does not implement production customer email.

## Migration

Added reversible migration:

- `014_secure_key_fulfillment`

Tables:

- `fulfillment_operations`
- `fulfillment_secrets`

The schema stores only encrypted secret material and safe metadata. It does not
add plaintext key, raw supplier response, supplier response JSON or
customer-visible key-copy columns.

## Encryption

Algorithm:

- `AES-256-GCM-v1`

Design:

- random data encryption key per secret;
- random 12-byte nonce per secret;
- authentication tag;
- canonical AAD binding fulfillment ID, supplier ID, external supplier order
  ID, purpose, algorithm and version;
- wrapped data encryption key;
- explicit `encryptionKeyId`;
- explicit `encryptionVersion = 1`.

Runtime configuration:

- `KEYCORE_FULFILLMENT_MASTER_KEY`
- `KEYCORE_FULFILLMENT_MASTER_KEY_ID`
- `KINGUIN_CONTROLLED_KEY_RETRIEVAL_TIMEOUT_MS`, default `10000`

The master key must be base64-encoded 32-byte material. Missing or malformed
configuration fails closed. No real key is committed and no production key is
generated silently.

Key rotation readiness is provided by persisted key ID, version and wrapped DEK.
Full rotation workflow remains future work.

## Controlled Retrieval Gates

Preparation command:

```sh
npm run kinguin:prepare-live-key-retrieval -- <controlledProcurementApprovalId>
```

Preparation is DB-only and performs zero supplier key requests.

Execution command:

```sh
npm run kinguin:execute-approved-key-retrieval -- <fulfillmentApprovalId> <oneTimeToken>
```

Execution requires:

- `KEYCORE_ALLOW_KINGUIN_LIVE_KEY_RETRIEVAL=true`;
- `KEYCORE_KINGUIN_CONTROLLED_KEY_RETRIEVAL_MODE=CONTROLLED_VERIFICATION_ONE_TIME`;
- correct unexpired one-time token;
- confirmed controlled procurement;
- known external supplier order ID;
- supplier `kinguin`;
- no existing encrypted secret;
- valid crypto configuration;
- retrieval lease ownership.

The prior read-only and purchase-mutation flags do not enable key retrieval.
One execution invocation performs at most one supplier key request and there is
no internal retry loop.

## Crash And Local Failure Behavior

Kinguin retrieval and local PostgreSQL persistence are not modeled as one atomic
transaction. After key material is received, KeyCore encrypts immediately and
then commits the encrypted secret insert plus fulfillment transition in one
database transaction. Mutable plaintext key buffers are overwritten as soon as
the post-response processing finishes.

Because current Kinguin docs support repeated read-only key download, recovery
from post-response local failures can be `FAILED_RETRYABLE`. Classification is
local, not supplier-blaming:

- KMS/encryption failure: `FULFILLMENT_KEY_MANAGEMENT_FAILED`;
- local encrypted persistence/ownership loss: `FULFILLMENT_LOCAL_PERSISTENCE_FAILED`;
- unknown local exception before a classified supplier result:
  `FULFILLMENT_LOCAL_UNKNOWN` with `AMBIGUOUS`.

Successful encrypted persistence now returns and audits
`FULFILLMENT_KEY_RETRIEVED`. `FULFILLMENT_CREATED` is used only for operation
creation.

## Idempotency And Concurrency

Fulfillment creation is idempotent per controlled procurement approval. The
database enforces one fulfillment operation per controlled approval and one
secret per fulfillment operation.

Retrieval execution uses a durable lease:

- one worker acquires `RETRIEVAL_IN_FLIGHT`;
- concurrent workers receive in-flight state;
- stale retryable retrieval can be reacquired;
- already retrieved fulfillment cannot retrieve again.

`PostgresFulfillmentRepository.markRetrieved()` locks the fulfillment operation
row, requires the current retrieval execution token and commits encrypted secret
insert plus operation transition atomically. If ownership is lost, no secret row
is committed.

## Audit And Outbox Restrictions

Audit and outbox payloads use safe IDs and state only. They exclude plaintext,
ciphertext, nonce, tag, wrapped DEK, master key material, one-time tokens, token
hashes and raw supplier responses.

## Safe Inspect Command

Added:

```sh
npm run fulfillment:inspect -- <fulfillmentId>
```

The command reads local PostgreSQL only and prints safe metadata. It does not
contact Kinguin and does not print plaintext or encrypted bytes.

## Real Order Compatibility Check

DB-only command executed:

```sh
npm.cmd run kinguin:inspect-controlled-procurement -- 18fd3323-c844-4625-8de3-68ab4e453dc0
```

Observed locally:

- approval ID: `18fd3323-c844-4625-8de3-68ab4e453dc0`;
- status: `PROCUREMENT_CONFIRMED`;
- dispatch state: `DISPATCH_CONFIRMED`;
- orderExternalId:
  `keycore-liveverify-aae4a9f6-7cb8-4049-a0c2-6db282e4daae`;
- externalSupplierOrderId: `GE1373B866F3`;
- supplier: `Kinguin`.

This command did not contact Kinguin and did not retrieve keys.

## Tests

Focused local run:

```sh
npm.cmd test -- packages/platform/src/fulfillment/secure-key-fulfillment.test.ts infra/suppliers/kinguin/kinguin-key-retrieval.test.ts infra/postgres/fulfillment-persistence.test.ts
```

Result:

- 25 passed;
- 7 skipped locally because `KEYCORE_TEST_DATABASE_URL` was unset.

Covered:

- AES-GCM round trip;
- random nonce and ciphertext;
- tampered ciphertext/tag failure;
- wrong key failure;
- crypto version validation;
- AAD ownership binding;
- no secret in AAD;
- key ID persistence;
- missing crypto config fail closed;
- prepare makes zero key requests;
- wrong token rejection;
- expired token rejection;
- single-use execution;
- concurrent execution lease ownership;
- successful fake retrieval encryption;
- success reason `FULFILLMENT_KEY_RETRIEVED`;
- `DELIVERY_PENDING` distinct from `DELIVERED`;
- duplicate retrieval blocked;
- one supplier request per execution invocation;
- documented Kinguin repeatable read-only retrieval policy;
- controlled key retrieval timeout default and invalid timeout rejection;
- zero/multiple/malformed/pending supplier responses;
- supplier error mapping;
- local KMS/encryption failure classification;
- local persistence failure classification;
- unknown local exception ambiguity;
- plaintext buffer zeroing after processing;
- canary leakage checks;
- Kinguin key retrieval guard;
- PostgreSQL idempotency, uniqueness, lease, atomic secret insert, orphan-secret
  prevention, concurrent `markRetrieved` and no-plaintext-column coverage when
  the database is available.

Full local run:

```sh
npm.cmd run check
```

Result:

- format: passed;
- lint: passed;
- typecheck: passed;
- tests: 465 passed, 85 skipped;
- secret scan: passed.

Additional local gates:

```sh
npm.cmd audit --audit-level=low
npm.cmd run secrets:scan
```

Result:

- npm audit: found 0 vulnerabilities;
- secret scan: passed.

Local PostgreSQL integration tests were skipped in the full run because
`KEYCORE_TEST_DATABASE_URL` was not set in the process environment. GitHub
Actions provides the PostgreSQL service container and is expected to run them.

## CI Follow-Up

GitHub Actions initially exposed a PostgreSQL-only fixture defect: fulfillment
operations with a controlled procurement approval ID were inserted without the
referenced `controlled_procurement_approvals` row. The tests were corrected to
create the approval through the real repository before inserting fulfillment
operations. The 50k metadata test was also changed to use PostgreSQL
`generate_series` instead of a large client-side `VALUES` statement, preserving
the same acceptance coverage while avoiding CI timeout risk.

## Pre-Merge Hardening

Before merge, KS-07-04 was hardened for retry, crash, timeout and audit
behavior:

- official Kinguin repeatability evidence was clarified: periodic key download
  with pagination is documented, while key return is a separate POST mutation;
- controlled key retrieval timeout is configurable through
  `KINGUIN_CONTROLLED_KEY_RETRIEVAL_TIMEOUT_MS`, default `10000`;
- one execute invocation performs at most one supplier key request;
- successful encrypted persistence uses `FULFILLMENT_KEY_RETRIEVED`;
- `FULFILLMENT_CREATED` is reserved for operation creation;
- local KMS/encryption failures use `FULFILLMENT_KEY_MANAGEMENT_FAILED`;
- local encrypted persistence failures use
  `FULFILLMENT_LOCAL_PERSISTENCE_FAILED`;
- unknown local exceptions are `AMBIGUOUS` with `FULFILLMENT_LOCAL_UNKNOWN`,
  not supplier-retryable;
- mutable plaintext key buffers are overwritten after post-response processing;
- PostgreSQL `markRetrieved()` locks the operation row and commits encrypted
  secret insert plus state transition atomically;
- stale/competing writers cannot persist a second secret or leave an orphan
  secret row.

Hardening local focused run:

```sh
npm.cmd test -- packages/platform/src/fulfillment/secure-key-fulfillment.test.ts infra/suppliers/kinguin/kinguin-key-retrieval.test.ts infra/postgres/fulfillment-persistence.test.ts
```

Result:

- 25 passed;
- 7 skipped locally because `KEYCORE_TEST_DATABASE_URL` was unset.

## Known Limitations

- Production customer email is not implemented.
- Broad admin plaintext reveal is not implemented.
- Full key rotation workflow is not implemented.
- Normal customer-order fulfillment wiring remains future work.
- Real live retrieval must wait for merge, local migration and explicit
  operator action.

## Safety Confirmations

- LIVE KEY RETRIEVAL COMMAND NOT RUN.
- NO REAL PRODUCT KEY RETRIEVED.
- NO PRODUCT KEY DISPLAYED.
- NO PRODUCT KEY COMMITTED.
- NO CUSTOMER DELIVERY PERFORMED.
- NO ADDITIONAL PURCHASE PERFORMED.
- KS-07-04 ONLY.
