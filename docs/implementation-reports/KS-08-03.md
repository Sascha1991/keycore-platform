# KS-08-03 Implementation Report

## Summary

Implemented the Customer Account Transport & Shop Integration Foundation as a
transport-neutral TypeScript application boundary. No production HTTP server,
WordPress shop, WooCommerce dependency, frontend, real login provider, real key
reveal, Kinguin call or live Stripe mutation was added.

Branch: `feature/ks-08-03-customer-account-transport`

## Files Added Or Changed

- `packages/platform/src/customers/customer-account-transport.ts`
- `packages/platform/src/customers/customer-account-transport.test.ts`
- `packages/platform/src/fulfillment/authenticated-customer-delivery.ts`
- `packages/platform/src/contracts.ts`
- `docs/customer-account-api.md`
- `docs/woocommerce-customer-account-integration.md`
- `docs/customer-account.md`
- `docs/customer-registration.md`
- `docs/customer-authentication.md`
- `docs/authenticated-customer-delivery.md`
- `docs/implementation-reports/KS-08-03.md`
- `ROADMAP.md`
- `CHANGELOG.md`

## Migration

Migration created: NO.

Existing KS-07-07, KS-08-01 and KS-08-02 persistence is sufficient for this
transport/application boundary.

## Transport Architecture

The new `CustomerAccountTransportHandler` defines a v1 DTO boundary for future
HTTP, Keyrano storefront or WooCommerce adapter composition:

```text
request cookie/header -> opaque session token -> KS-07-07 session resolver
-> AuthenticatedCustomerPrincipal -> account/registration service -> safe DTO
```

The transport rejects request-supplied authority fields such as `customerId`,
`providerSubject`, `orderOwner`, supplier order IDs, fulfillment references and
delivery capabilities where they do not belong.

## Handler Models

Account handler model:

- `GET` account summary resolves a session before calling account service.
- `GET` owned order history validates bounded query params and opaque cursor
  shape before session resolution.
- `GET` owned order detail validates UUID path params and calls
  `getOwnedOrderDetail`; it does not load arbitrary orders directly.

Registration handler model:

- `POST` registration validates strict body `{ email }`.
- Public success remains `REGISTRATION_ACCEPTED`.
- Existing and new email addresses share the same public response.
- No session is created automatically.

Verification handler model:

- `POST` verification validates strict body `{ verificationToken }`.
- Token is treated as secret input.
- Token is never echoed in responses or audit assertions.
- Invalid, consumed and expired tokens collapse to `VERIFICATION_INVALID`.

Identity-link handler model:

- `POST` identity link requires authenticated session, Origin and CSRF checks.
- Body must be empty.
- Trusted provider evidence remains injected through
  `CustomerIdentityBindingAuthorityPort`.
- Raw browser `providerSubject` is rejected.

## Security Policies

Session extraction/resolution:

- session credential is opaque;
- credential extraction is explicit and deterministic across direct session
  credential, cookie, Authorization Bearer and session-header sources;
- duplicate credentials, duplicate credential sources, whitespace-padded
  credentials, empty credentials, oversized credentials, malformed opaque tokens
  and conflicting sources fail closed with `AUTHENTICATION_REQUIRED`;
- `customerId` request fields are never accepted as principal source;
- missing, malformed, expired and revoked sessions return
  `AUTHENTICATION_REQUIRED`;
- denied sessions do not call account repositories.

Cookie policy:

- `HttpOnly; Secure; SameSite=Lax; Path=/`;
- no token in URLs;
- no localStorage recommendation.

CSRF policy:

- authenticated mutations reuse the KS-07-08 HMAC double-submit CSRF policy;
- SameSite is not the only mutation defense.

Origin/CORS policy:

- KS-08-03 reuses the KS-07-08 origin normalizer;
- production/staging require configured HTTPS origins;
- local/CI may allow deliberate localhost HTTP;
- wildcard credentialed CORS remains forbidden.

Rate-limit reuse:

- transport uses the KS-07-08-style `AuthenticatedCustomerDeliveryRateLimiter`
  port;
- sensitive limiter outage returns `TEMPORARILY_UNAVAILABLE`;
- in-memory limiter remains local/test only.

Cache and response headers:

- account reads use `Cache-Control: private, no-store`;
- mutations use `Cache-Control: no-store`;
- token-sensitive verification uses `Referrer-Policy: no-referrer`;
- all JSON responses include `X-Content-Type-Options: nosniff`.

Error mapping:

- stable public codes map to 400, 401, 403, 404, 409, 429 and 503;
- internal exceptions, SQL, provider errors and stack traces are not returned.

Request schema strictness:

- mutation bodies are strict;
- dangerous or unknown authority fields are rejected, including customer,
  provider, supplier, fulfillment, verification-state, principal/session and
  delivery-capability fields;
- invalid content type, body size, UUID, cursor and limit fail closed.

Unexpected internal exceptions:

- account, registration, verification and identity-link service exceptions map
  to `TEMPORARILY_UNAVAILABLE`;
- SQL constraint names, filesystem paths, stack traces, provider errors and raw
  token material remain redacted from customer responses.

## WooCommerce Boundary

WooCommerce remains an adapter/client. It is not authoritative for KeyCore
customer identity, order ownership, verification state, key ownership or
delivery authorization.

The documented future adapter contract explicitly denies email-only
authentication, identity linking and order claiming. A future WooCommerce
identity bridge must provide signed/trusted server-side provider evidence and
must not pass arbitrary browser `providerSubject` as proof.

## Key Reveal Linkage

Account order detail may return safe key-vault metadata and
`keyAccessAvailable=true`, but it does not prepare, decrypt, display, deliver or
consume a delivery capability. Future reveal remains:

```text
account order detail -> explicit customer reveal action
-> KS-07-08 authenticated delivery transport
```

## Tests

Focused synthetic transport tests cover:

- session token to principal to account summary;
- duplicate, conflicting, empty, whitespace and oversized credentials;
- owned order history;
- owned order detail with safe key-vault metadata;
- missing, malformed, expired and revoked sessions;
- invalid request syntax before session resolution;
- exact production Origin normalization and deceptive Origin rejection;
- content-type and body-size failure before limiter/domain mutation;
- invalid limit/cursor and UUID validation;
- wrong-owner, unknown and legacy order indistinguishability;
- known real fulfillment remains inaccessible and untouched;
- forged `customerId` rejected;
- supplier order ID cannot select resources;
- security-sensitive DTO authority fields rejected;
- provider subject cannot link identity;
- registration accepted with stable no-enumeration response;
- existing email same public registration response;
- no session auto-created by registration;
- rate-limit failure and outage mapping;
- raw session token absent from limiter keys, responses and audit events;
- invalid correlation IDs sanitized and internal failures redacted;
- valid synthetic verification token verifies;
- consumed token returns stable invalid;
- token not echoed or audited through success, denial, rate-limit and failure
  paths;
- authenticated verified trusted identity linking;
- missing auth and unverified customer denial;
- raw provider subject rejection;
- WooCommerce email-only trust regression.

Synthetic transport E2E:

```text
session token -> principal -> account summary -> order history -> order detail
-> safe key-vault metadata
```

No decrypt and no delivery are performed.

Focused local result after PR #28 hardening:

- `npm.cmd test -- packages/platform/src/customers/customer-account-transport.test.ts packages/platform/src/customers/customer-account.test.ts packages/platform/src/customers/customer-registration.test.ts packages/platform/src/fulfillment/authenticated-customer-delivery.test.ts`
- 52 passed.

## PR #28 Hardening Review

Review finding: code changes were required.

The focused security review found four transport-boundary gaps and corrected
them on the existing branch:

- credential-source ambiguity was not represented explicitly enough for future
  cookie/header adapters;
- security-sensitive DTO rejection did not cover every requested authority
  field;
- unexpected service/repository exceptions could bubble past the stable
  transport error envelope;
- transport limit parsing rejected values above 100 instead of preserving the
  KS-08-01 service clamp.

Corrective behavior now in place:

- protected account handlers use only opaque session credential extraction,
  KS-07-07 session resolution and `AuthenticatedCustomerPrincipal`;
- invalid sessions reach zero account repository calls;
- malformed requests that can be rejected structurally reach zero session
  resolver calls;
- unknown, wrong-owner and legacy/unclaimed order access remains
  indistinguishable as `RESOURCE_NOT_AVAILABLE`;
- public registration and verification remain unauthenticated but require
  strict Origin, content-type, body-size and rate-limit controls;
- authenticated identity linking keeps Origin and CSRF checks before domain
  mutation;
- rate-limit keys use safe fingerprints rather than raw session, email or
  verification-token material;
- account DTOs expose only safe metadata and no key-vault cryptographic
  material;
- account read paths do not decrypt, deliver, call suppliers, retrieve Kinguin
  keys or consume delivery capabilities.

## Commands Executed

- `npm.cmd run format -- packages/platform/src/customers/customer-account-transport.ts packages/platform/src/customers/customer-account-transport.test.ts packages/platform/src/fulfillment/authenticated-customer-delivery.ts packages/platform/src/contracts.ts`
- `npm.cmd run typecheck`
- `npm.cmd test -- packages/platform/src/customers/customer-account-transport.test.ts packages/platform/src/customers/customer-account.test.ts packages/platform/src/customers/customer-registration.test.ts packages/platform/src/fulfillment/authenticated-customer-delivery.test.ts`
- `npm.cmd test -- infra/postgres/customer-account-persistence.test.ts infra/postgres/customer-registration-persistence.test.ts infra/postgres/customer-authentication-persistence.test.ts`
- `npm.cmd run check`
- `npm.cmd audit --audit-level=low`
- `npm.cmd run secrets:scan`
- `git diff --check`

Full local `npm run check` result: 546 passed, 91 skipped.

Focused local transport/account/registration/delivery result: 52 passed.

Focused local PostgreSQL result: 3 files skipped, 4 tests skipped because the
local PostgreSQL test database was not configured.

`npm audit --audit-level=low`: 0 vulnerabilities.

Secret scan: passed.

`git diff --check`: passed.

GitHub Actions status is to be recorded after push and PR creation.

## PostgreSQL

No PostgreSQL migration or repository behavior was changed. Existing
PostgreSQL integration tests are expected to run in CI through the existing
quality gates.

## Production Readiness

- REAL LOGIN PROVIDER CONNECTED: NO
- PRODUCTION CUSTOMER ACCOUNT API EXPOSED: NO
- WOOCOMMERCE CONNECTED: NO
- PRODUCTION FRONTEND CONNECTED: NO
- PRODUCTION EMAIL DELIVERY CONNECTED: NO
- PRODUCTION DISTRIBUTED RATE LIMITER READY: NO
- REAL KEY REVEAL ENABLED: NO

## Safety Confirmations

- NO REAL PRODUCT KEY DECRYPTED
- NO REAL PRODUCT KEY DISPLAYED
- NO REAL PRODUCT KEY DELIVERED
- NO FABRICATED OWNER ASSIGNED
- NO LEGACY ORDER CLAIMED
- NO NEW KINGUIN KEY RETRIEVAL
- NO NEW KINGUIN PURCHASE
- NO LIVE STRIPE MUTATION
- REAL FULFILLMENT `fd61be5e-44ea-4914-98ae-c4404dc31779` REMAINS DELIVERY_PENDING AND UNCLAIMED
- KS-08-04 / PHASE 09 NOT STARTED
