# KS-11-06 Implementation Report

## Summary

Implemented REC-001 through REC-018 as a release-blocking native PostgreSQL
recovery exercise. The suite restores representative durable state into a
separate database, validates it through SQL and application repositories,
simulates total Redis loss and creates an independent continuation order.

## Files Added

- `infra/recovery/recovery-exercise.ts`
- `infra/recovery/recovery-exercise.test.ts`
- `vitest.recovery-exercise.config.ts`
- `docs/recovery-exercise.md`
- `docs/recovery-runbook.md`
- `docs/implementation-reports/KS-11-06.md`
- `tasks/KS-11-06-recovery-exercise.md`

## Files Changed

- `.github/workflows/quality-gates.yml`
- `package.json`
- `tsconfig.json`
- `vitest.config.ts`
- `ROADMAP.md`
- `CHANGELOG.md`
- `docs/dependency-matrix.md`
- `docs/phase-11-acceptance-matrix.md`
- `docs/acceptance-test-matrix.md`
- `docs/testing/test-strategy.md`
- `docs/security-residual-risk-register.md`

## Architecture And Safety

No production business module, migration or database constraint changed. The
migration baseline remains 027. Tool credentials are child-process environment
variables, never arguments or evidence. Tool errors become stable safe reason
codes and raw stderr is not emitted. Both databases and the dump are always
removed. No external provider adapter is instantiated.

## Recovery Findings

No recovery finding requiring production remediation or migration has been
identified during implementation. Critical: 0; High: 0; Medium: 0; Low: 0;
Info: 0; Blocking: 0. Quality Gates #170 accepted this result for commit
`880ccd83761471e303d0a1e82f754828718ffefd`.

## Verification

- Typecheck: passed.
- ESLint: passed.
- Local `npm run recovery:exercise`: guard test passed; native restore test
  explicitly skipped because local Redis is unavailable. PostgreSQL 18 and
  client tools are available locally; CI remains authoritative for the pinned
  PostgreSQL 16 plus Redis native exercise.
- Guest-claim concurrency test: 20 focused PostgreSQL executions passed with
  zero failures; each execution retained both ten-actor races.
- Local `npm run order:concurrency`: three complete PostgreSQL executions
  passed with zero failures, 38/38 tests each.
- Local `npm run e2e:acceptance`: passed, 16/16 tests.
- Local `npm run security:assessment`: passed, 60 tests with 342 tests excluded
  by the focused security selection.
- Local `npm run check`: formatting, lint, typecheck and 718 tests passed; the
  unchanged Windows 50,000-offer microbenchmark took 2258 ms against its 2000
  ms bound, and 124 PostgreSQL tests were disabled for this parallel run. The
  exact Guest-Claim test passed repeatedly and in the PostgreSQL suites above;
  Linux CI remains authoritative for the unchanged performance threshold.
- Local `npm audit --audit-level=low`: passed; 0 vulnerabilities.
- Local secret scan and `git diff --check`: passed.
- Quality Gates #170: passed. Full CI suite: 843 passed; KS-11-02: 16 passed;
  KS-11-03: 10 passed; KS-11-04: 38 passed; KS-11-05: 60 passed and 342
  skipped by its focused selection; KS-11-06: 2 passed.
- Native recovery evidence: `pg_dump 16.15`, custom-format backup size 269281
  bytes, backup 205 ms, restore 813 ms, validation 149 ms and total exercise
  1904 ms. REC-001 through REC-018 passed and all 18 post-restore invariant
  counts were zero.
- Evidence artifact: `ks-11-06-recovery-exercise-evidence`, five safe files,
  no raw backup and no raw secrets. The recovery log contained no overlapping
  PostgreSQL query or transaction warning originating from this exercise.

Initial Quality Gates #166 stopped in the unchanged concurrent guest-claim
integration test before any dedicated Phase-11 suite ran (842 passed, 1 failed).
Because prior KS-11-02 through KS-11-04 artifact uploads used unconditional
`always()`, their skipped suites produced misleading secondary missing-artifact
failures. KS-11-06 adds stable step IDs and archives each artifact only when its
suite was not skipped, matching the existing KS-11-05/06 behavior without
weakening any release gate.

Quality Gates #167 reached the native exercise and exposed a synthetic fixture
timestamp mismatch. Migration 025 creates emergency-control rows at migration
time, while the fixture had assigned an older fixed `updated_at`, correctly
violating `created_at <= updated_at`. The fixture now preserves the migration
`created_at` as its pause time, records matching pause-event times and uses the
actual exercise time for explicit post-restore resumes. No production code,
migration, constraint or recovery invariant was changed.

Quality Gates #168 then exposed an ambiguous `record_version` reference in the
synthetic guest-claim recovery statement because both joined tables define that
column. The update now explicitly targets the challenge alias. Safe row counts
and invariant counts are also collected sequentially so the source `pg.Client`
never receives overlapping queries; this removes the recovery-originated pg@9
deprecation warning without changing what is validated.

Quality Gates #169 attempt 2 reached Redis-loss validation and identified an
incorrect fixture expectation: it required five orders before the fifth,
post-recovery continuation order is intentionally created. The guard now
captures PostgreSQL order count and global-control state immediately before
`FLUSHDB`, then proves both remain identical and paused afterward. This tests
durable-state survival directly and retains the explicit-authority requirement
before creating the continuation order.

Quality Gates #171 exposed a pre-existing CONC-014 race before KS-11-05/06:
`FOR UPDATE SKIP LOCKED` allowed a mismatched claimant to hold the challenge
while every matching claimant skipped it, producing ten safe denials but no
winner. Guest-claim consumption now waits on the exact row using independent
transactions. The focused assertion records the token-free distribution and
requires exactly one `CLAIMED`, nine `CLAIM_DENIED`, one consumed challenge and
the matching immutable owner. Recovery behavior and REC-001 through REC-018 are
unchanged.

## Residual Risk

Production backup storage/scheduling/retention, restore authority,
infrastructure failover, KMS recovery, observability/escalation, provider and
WooCommerce recovery, and RTO/RPO approval remain Phase 12. Human workflow/UAT
remains KS-11-07. Synthetic isolated CI scope is accepted for this phase.

## Status

KS-11-05 is complete and merged. KS-11-07 was not started. Phase 11 remains
incomplete and `SECURITY-READINESS` remains unapproved. This PR must remain
open and unmerged for review.
